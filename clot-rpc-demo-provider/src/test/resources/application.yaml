server:
  port: 8082

clotrpc:
  zk:
    server: localhost:2181
    root: clot-rpc
  app:
    id: app1
    namespace: public
    env: dev
  provider:
    metas:
      dc: bj # 机房
      gray: false # 是否灰度
      unit: B001 # 区域

logging:
  level:
    root: error
    cn.cutie.clotrpc: info